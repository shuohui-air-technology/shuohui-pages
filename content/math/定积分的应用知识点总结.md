---
title: 定积分的应用知识点总结
date: 2026-06-16T17:03:00
math: true
draft: false
comments: true
cover: null
---

# 定积分的几何应用学习笔记

### 一、 平面图形的面积计算

定积分计算平面图形面积的核心思想是面积微元法（将整体图形分割为无数个微小的规则图形进行累加）。

1. **直角坐标系面积**
当图形由上边界曲线 $y=f(x)$ 与下边界曲线 $y=g(x)$ 以及两条垂直直线 $x=a$ 和 $x=b$ 围成时，面积公式为：

$$A = \int_{a}^{b} |f(x) - g(x)| dx$$

若图形关于 $y$ 轴方向更容易表达（即左边界为 $x=h(y)$ ，右边界为 $x=k(y)$ ），则应转化为对 $y$ 的定积分：

$$A = \int_{c}^{d} [k(y) - h(y)] dy$$

2. **参数方程面积**
对于由参数方程 $x=x(t)$ 与 $y=y(t)$ 给出的闭合或非闭合曲线，需要利用换元法将直角坐标积分转化为对参数 $t$ 的定积分。若随着 $t$ 从 $t_1$ 变到 $t_2$ ，自变量 $x$ 从 $a$ 单调变到 $b$ ，则面积公式为：

$$A = \int_{t_1}^{t_2} |y(t) x'(t)| dt$$

3. **极坐标系面积**
对于由极坐标方程 $r=r(\theta)$ 以及两条极角射线 $\theta=\alpha$ 和 $\theta=\beta$ 围成的扇形图形，面积微元是面积为 $\frac{1}{2}r^2 d\theta$ 的小扇形。其积分公式为：

$$A = \frac{1}{2} \int_{\alpha}^{\beta} [r(\theta)]^2 d\theta$$

### 二、 旋转体体积的计算

计算旋转体体积时，需要根据旋转轴的位置以及平面图形的边界特征，选择最合适的体积微元构造方式。

1. **圆盘法与圆环法**
适用于平面图形绕自身边界或与其平行的坐标轴旋转的情形。

    - 当连续曲线 $y=f(x) \ge 0$ 与 $x=a$ ， $x=b$ 以及 $x$ 轴围成的图形绕 $x$ 轴旋转时，体积微元为厚度为 $dx$ 、半径为 $y$ 的圆盘，公式为：

$$V_x = \pi \int_{a}^{b} [f(x)]^2 dx$$

- 当图形由两条曲线 $y=f(x)$ 和 $y=g(x)$ （其中 $f(x) \ge g(x) \ge 0$ ）围成并绕 $x$ 轴旋转时，体积微元变为圆环，公式为：

$$V_x = \pi \int_{a}^{b} \left([f(x)]^2 - [g(x)]^2\right) dx$$

2. **柱壳法（圆柱壳法）**
平面图形绕 $y$ 轴旋转，但边界函数采用 $y=f(x)$ 形式且难以反解出 $x$ 时，通常采用柱壳法。将图形分割为无数个半径为 $x$ 、高度为 $f(x)$ 、厚度为 $dx$ 的薄圆柱壳，公式为：

$$V_y = 2\pi \int_{a}^{b} x f(x) dx$$

3. **参数方程与极坐标曲线的旋转体积**

    - 对于参数方程给出的图形，需将体积公式中的自变量与因变量均用参数 $t$ 的表达式替换，并相应地改写积分限与微分项。
    - 对于极坐标曲线 $r=r(\theta)$ 绕极轴旋转所得立体的体积，通常先利用直角坐标与极坐标的转换关系 $x=r(\theta)\cos\theta$ ， $y=r(\theta)\sin\theta$ 将其转化为参数方程形式，再代入绕 $x$ 轴旋转的体积公式中进行计算。

### 三、 曲线弧长的计算

弧长计算的关键在于正确写出不同坐标系下的弧长微元 $ds$ 。

1. **直角坐标系弧长**

    - 对于显式光滑曲线 $y=f(x)$ （ $a \le x \le b$ ），弧长微元为 $ds = \sqrt{1 + [f'(x)]^2} dx$ ，积分公式为：

$$s = \int_{a}^{b} \sqrt{1 + [f'(x)]^2} dx$$

- 对于隐函数方程（如 $\sqrt{x}+\sqrt{y}=1$ ），需先通过隐函数求导法或方程变形求出导数 $y'$ ，再代入上述公式。

2. **参数方程弧长**
对于光滑参数曲线 $x=x(t)$ ， $y=y(t)$ （ $t_1 \le t \le t_2$ ），弧长微元为 $ds = \sqrt{[x'(t)]^2 + [y'(t)]^2} dt$ ，积分公式为：

$$s = \int_{t_1}^{t_2} \sqrt{[x'(t)]^2 + [y'(t)]^2} dt$$

3. **极坐标系弧长**
对于光滑极坐标曲线 $r=r(\theta)$ （ $\alpha \le \theta \le \beta$ ），弧长微元为 $ds = \sqrt{[r(\theta)]^2 + [r'(\theta)]^2} d\theta$ ，积分公式为：

$$s = \int_{\alpha}^{\beta} \sqrt{[r(\theta)]^2 + [r'(\theta)]^2} d\theta$$

### 四、 旋转体表面积的计算

旋转曲面的面积可以理解为曲线的弧长微元 $ds$ 绕指定轴旋转一周所扫过的侧面积累加。

1. **直角坐标系表面积**
光滑曲线 $y=f(x) \ge 0$ （ $a \le x \le b$ ）绕 $x$ 轴旋转所得旋转面的面积公式为：

$$S_x = 2\pi \int_{a}^{b} f(x) \sqrt{1 + [f'(x)]^2} dx$$

_注：若旋转轴不是坐标轴而是平行于坐标轴的直线（如 $x^2+(y-a)^2=r^2$ 绕 $x$ 轴旋转），需要根据几何关系将公式中的旋转半径 $f(x)$ 修正为点到旋转轴的实际距离。_

2. **参数方程表面积**
光滑参数曲线 $x=x(t)$ ， $y=y(t) \ge 0$ （ $t_1 \le t \le t_2$ ）绕 $x$ 轴旋转所得旋转面的面积公式为：

$$S_x = 2\pi \int_{t_1}^{t_2} y(t) \sqrt{[x'(t)]^2 + [y'(t)]^2} dt$$

3. **极坐标系表面积**
光滑极坐标曲线 $r=r(\theta) \ge 0$ （ $\alpha \le \theta \le \beta$ ）绕极轴（即 $x$ 轴正方向）旋转时，平面上点的旋转半径为 $y = r(\theta)\sin\theta$ 。结合极坐标下的弧长微元，其表面积积分公式为：

$$S = 2\pi \int_{\alpha}^{\beta} r(\theta)\sin\theta \sqrt{[r(\theta)]^2 + [r'(\theta)]^2} d\theta$$

### 五、 核心基础技能与高级计算技巧

1. **特殊曲线的几何特征与对称性**

    - **经典曲线草图**：必须熟练掌握内摆线（星形线 $x=a\cos^3 t, y=a\sin^3 t$ ）、心形线（ $r=a(1+\cos\theta)$ ）、三叶形曲线（ $r=a\sin 3\theta$ ）、双纽线（ $r^2=2a^2\cos 2\theta$ ）和摆线（ $x=a(t-\sin t), y=a(1-\cos t)$ ）等特殊图形的几何形态与参数/极坐标范围。
    - **利用对称性简化计算**：在计算面积、弧长或体积时，应优先观察图形关于坐标轴、原点或极轴的对称性。通过只计算第一象限或半个周期的定积分，再乘以相应的倍数，可以极大地简化积分上限与下限的带入，大幅降低计算复杂度，减少出错率。

2. **复杂定积分的计算技巧**
在处理弧长与表面积的定积分时，被积函数中高概率会出现包含根号的复杂结构。需要熟练运用以下高级运算工具：

    - **三角换元**：针对 $\sqrt{a^2-x^2}$ 、 $\sqrt{a^2+x^2}$ 等根式，通过令 $x=a\sin\theta$ 或 $x=a\tan\theta$ 脱去根号。
    - **三角恒等变形**：灵活运用倍角公式与半角公式进行降次或开方。例如，利用 $1-\cos t = 2\sin^2\frac{t}{2}$ 或 $1+\cos\theta = 2\cos^2\frac{\theta}{2}$ 将根号内部化为完全平方式以消除根号。
    - **华里士公式（Wallis formula）**：在对称区间或特定三角积分区间下，熟练使用华里士公式快速求解形如 $\int_{0}^{\frac{\pi}{2}} \sin^n x dx$ 或 $\int_{0}^{\frac{\pi}{2}} \cos^n x dx$ 的积分，避免繁琐的逐次积分项推导。

3. **交点与积分限的判定**
4. **精准寻找交点**：对于由两道或多道曲线围成的复合图形，或者涉及多条曲线相交的求公共面积/体积问题（如 $r=\sin\theta$ 与 $r=\sqrt{3}\cos\theta$ 的公共部分），绝对不能盲目猜测积分限。必须通过联立方程组（如联立代数方程或极坐标方程）精确求解交点坐标或交点的极角 $\theta$ 。**区间分割与边界判定**：依据交点将整个积分区域划分为若干个子区间，并在每个子区间内准确判定函数的上下边界或内外边界，从而确立正确的积分上限与下限。

对应的图形（generated by trae）:
星形线     星形线 $ \begin{cases} x = a \cos^3 t \ y = a \sin^3 t \end{cases} $ $t \in [0, 2\pi]$

![](/images/astroid.png)

心形线 $r=a(1+\cos\theta)$  $\theta$-$[0, 2\pi]$

![](/images/cardioid.png)

三叶形曲线 $$r = a \sin 3\theta$$  $\theta$-$[0, \pi]$

![](/images/three_leaf_rose.png)

双纽线 $r^2 = 2a^2 \cos 2\theta$/$$(x^2 + y^2)^2 = 2a^2(x^2 - y^2)$$

![](/images/lemniscate.png)

摆线  $ \begin{cases} x = a(t - \sin t) \ y = a(1 - \cos t) \end{cases} $ $t \in [0, 2\pi]$

定积分几何应用中积分上下限的确定方法

### 一、 直角坐标系中上下限 $a, b$ 的求法（以变量 $x$ 为例）

- **题目直接给出**：如 $y=x^{3/2}$ ， $0 \le x \le 4$ ，积分下限直接取 $0$ ，上限取 $4$ 。
- **联立方程求交点**：若图形由两条曲线围成，需通过联立方程组求解交点的横坐标。
例如：求由抛物线 $y=x^2$ 与 $y=2-x^2$ 所围图形的面积。令两个 $y$ 的表达式相等：

$$x^2 = 2 - x^2 \implies 2x^2 = 2 \implies x = \pm 1$$

解得交点横坐标为 $-1$ 和 $1$ ，即积分下限为 $-1$ ，上限为 $1$ 。

### 二、 参数方程中参数上下限 $t_1, t_2$ 的求法

当曲线由参数方程 $x=x(t), y=y(t)$ 给出时，确定参数 $t$ 的起始值 $t_1$ 和终点值 $t_2$ 通常有以下途径：

- **周期与几何意义判定法（整体图形）**：部分曲线具有明显的几何周期性。
    - **摆线**：摆线 $x=a(t-\sin t), y=a(1-\cos t)$ 中，圆无滑动地滚动一圈恰好形成一个完整的拱形，对应的参数范围为 $t \in [0, 2\pi]$ 。
    - **星形线/心形线**：完整绕原点一周闭合的全图，其参数范围通常为 $[0, 2\pi]$ 。
- **坐标映射反推法（局部或特定区间）**：若已知直角坐标的几何边界，可通过代入方程反解出相应的参数 $t$ 。
例如：计算星形线 $x=a\cos^3 t, y=a\sin^3 t$ 在第一象限的部分。在第一象限内，横坐标 $x$ 从最右侧顶点 $a$ 减少到最上方顶点 $0$ ：
    - 当 $x = a$ 时： $a\cos^3 t = a \implies \cos t = 1 \implies t_1 = 0$
    - 当 $x = 0$ 时： $a\cos^3 t = 0 \implies \cos t = 0 \implies t_2 = \frac{\pi}{2}$
由此得到第一象限对应的参数积分限为 $[0, \frac{\pi}{2}]$ 。在使用面积公式 $\int_{t_1}^{t_2} y(t)x'(t) dt$ 时，必须注意积分上下限的顺序需严格对应自变量 $x$ 从左到右（从小到大）的走向。

### 三、 极坐标系中角度上下限 $\alpha, \beta$ 的求法

在极坐标面积积分 $\int_{\alpha}^{\beta} \frac{1}{2}r^2 d\theta$ 中，确定极角 $\theta$ 的范围主要看图形是如何被射线扫描出来的：

- **零点扫掠法（针对经过极点的花瓣图形）**：通过令 $r=0$ 求解出来的角度即为图形的边界夹角。
例如：求三叶形曲线 $r=a\sin 3\theta$ 单片叶子的面积。令 $r = 0$ ：

$$\sin 3\theta = 0 \implies 3\theta = 0, \pi, 2\pi, \dots$$

取最邻近的两个正解，得到 $\theta_1 = 0$ 且 $\theta_2 = \frac{\pi}{3}$ ，因此计算单片叶子面积的积分限为 $[0, \frac{\pi}{3}]$ 。

- **极坐标交点法（针对多图相交的公共部分）**：
例如：求 $r=\sin\theta$ 与 $r=\sqrt{3}\cos\theta$ 所围公共部分的面积。联立方程求交点：

$$\sin\theta = \sqrt{3}\cos\theta \implies \tan\theta = \sqrt{3} \implies \theta = \frac{\pi}{3}$$

此交点极角将图形区域分为两部分，需在不同区间内对不同边界分别积分：区间 $[0, \frac{\pi}{3}]$ 内的外边界由 $r=\sin\theta$ 决定；区间 $[\frac{\pi}{3}, \frac{\pi}{2}]$ 内的外边界由 $r=\sqrt{3}\cos\theta$ 决定。

### 四、 利用几何对称性简化积分上下限

利用图形的对称性，可以通过只计算部分区域的定积分再乘以相应的倍数，以此简化积分上限和下限的带入（如优先利用 $0$ 、 $\frac{\pi}{2}$ 、 $\pi$ 等易于计算的特殊值），并规避正负号干扰。

- **星形线**：四个象限完全对称。可直接计算第一象限 $t \in [0, \frac{\pi}{2}]$ 的积分，最终结果乘以 $4$ 。
- **心形线**：关于极轴（ $x$ 轴）上下对称。可直接计算上半部分 $\theta \in [0, \pi]$ 的积分，最终结果乘以 $2$ 。
- **三叶形曲线**：三片叶子完全对称。可直接计算一片叶子 $\theta \in [0, \frac{\pi}{3}]$ 的积分，最终结果乘以 $3$ 。
